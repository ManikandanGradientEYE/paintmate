// Seed data only. Left empty on purpose: real accounts are added from
// /admin/connect, and the footer hides the "Connect with us" row until there is
// at least one — better than linking people to a platform's front page.
export const socialLinks: { platform: string; url: string }[] = [];
